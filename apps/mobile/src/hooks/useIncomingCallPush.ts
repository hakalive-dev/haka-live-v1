/**
 * @deprecated Video and account notification opens are handled by usePushNotificationOpen.
 * Kept as a no-op so existing imports do not break.
 */
export function useIncomingCallPush(_enabled: boolean) {
  /* see usePushNotificationOpen */
}
